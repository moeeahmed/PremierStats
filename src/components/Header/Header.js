import React from "react";
import NavBar from "../NavBar/Navbar";

const Header = () => {
  return (
    <React.Fragment>
      <header className="header">
        <NavBar />
      </header>
    </React.Fragment>
  );
};

export default Header;
